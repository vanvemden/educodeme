import styled from 'styled-components';

import { InputLabel as BaseInputLabel } from '../../../components/Input';

const StyledInputLabel = styled(BaseInputLabel)``;

export default StyledInputLabel;
