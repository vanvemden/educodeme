import styled from 'styled-components';

import { ConnectionSignal as BaseConnectionSignal } from 'components/Output/';

const StyledConnectionSignal = styled(BaseConnectionSignal)``;

export default StyledConnectionSignal;
