import styled from 'styled-components';

import { PrimaryButton as BasePrimaryButton } from '../../../components/Button';

const StyledPrimaryButton = styled(BasePrimaryButton)``;

export default StyledPrimaryButton;
