import styled from 'styled-components';

import { SignalIndicator as BaseSignalIndicator } from '../../../components/Output';

const StyledSignalIndicator = styled(BaseSignalIndicator)``;

export default StyledSignalIndicator;
